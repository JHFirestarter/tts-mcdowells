<%= form_for(@side) do |f| %>
  <% if @side.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@side.errors.count, "error") %> prohibited this side from being saved:</h2>

      <ul>
      <% @side.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :descrip %><br>
    <%= f.text_area :descrip %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
